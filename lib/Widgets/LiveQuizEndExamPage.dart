import 'package:flutter/material.dart';

import '../../models/PlayerModel.dart';

class LiveQuizEndExamPage extends StatelessWidget {
  PlayerModel player1;
  PlayerModel player2;
  LiveQuizEndExamPage({
    Key? key,
    required this.player1,
    required this.player2,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
